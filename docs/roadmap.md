# Roadmap — незакрытое

1. **Полный путь с реальным `EncryptedKey` от Контур.ОФД** (а не самодельным CMS-конвертом) —
   ждёт организационного доступа к тестовой площадке (`Endpoints.rst`).
2. Не проверено на практике: код ошибки `0x8009200C` (`CRYPT_E_NO_DECRYPT_CERT`) как единственный
   код на «нет подходящего ключа»; принимает ли `Decrypt()` Base64 с переносами строк/
   PEM-заголовками (сейчас передаётся «чистый» Base64 без переносов).
3. **Упаковка/публикация** — публиковать ли в npm под `@dudev/kontur-ofd-js-auth`, или пока
   достаточно git-зависимости (`"kontur-ofd-js-auth": "github:dudev/kontur-ofd-js-auth"`). Решить,
   когда появится первый реальный consumer (relsy frontend, `admin/ofd-auth/*`).
